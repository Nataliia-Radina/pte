$('.main_image').slidesjs({
    width: 949,
    height: 449,
    play: {
        effect: "fade",
        speed: 1000,
        interval: 5000,
        auto: true,
        // [boolean] Start playing the slideshow on load.
        pauseOnHover: false
    },
    navigation: {
        active: false,
        effect: "fade"
    }
});